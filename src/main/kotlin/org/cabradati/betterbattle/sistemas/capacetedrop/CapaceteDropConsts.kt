package org.cabradati.betterbattle.sistemas.capacetedrop

import org.cabradati.betterbattle.App

class CapaceteDropConsts {

    companion object {
        private const val PREFIX = App.SYSTEMAS_PREFIX + "capacetedrop."
        const val ATIVAR_SISTEMA = PREFIX + "enabled"
        const val CHANCE = PREFIX + "chance"
    }

}